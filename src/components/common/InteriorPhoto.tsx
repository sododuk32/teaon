import React from 'react';
import Image from 'next/image';

type photoInfo = {
	pname: string;
};

function InteriorPhoto({ pname }: photoInfo) {
	return <div></div>;
}

export default InteriorPhoto;
